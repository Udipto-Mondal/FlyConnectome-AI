version: '3.8'

services:
  flyconnectome-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flyconnectome_ai_app
    ports:
      - "8000:8000"
    environment:
      - DEBUG=False
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
    restart: unless-stopped

version: '3.8'

services:
  neurograph-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: neurograph_ai_app
    ports:
      - "8000:8000"
    environment:
      - DEBUG=False
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
    restart: unless-stopped
